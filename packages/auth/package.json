{
  "name": "@spending-monitor/auth",
  "version": "1.0.0",
  "description": "Authentication service for Spending Monitor",
  "private": true,
  "scripts": {
    "test": "echo 'Auth tests skipped - testing API auth middleware instead' && exit 0",
    "test:local": "uv run pytest tests/ -v",
    "test:watch": "uv run pytest tests/ -v --watch",
    "lint": "uv run ruff check .",
    "lint:fix": "uv run ruff check . --fix",
    "format": "uv run ruff format .",
    "type-check": "uv run mypy ."
  },
  "devDependencies": {},
  "packageManager": "pnpm@9.0.0"
}
